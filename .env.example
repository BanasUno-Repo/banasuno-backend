# Redis (default: redis://localhost:6379)
REDIS_URL=redis://localhost:6379

# Path to Davao health facilities JSON when seeding (npm run seed:facilities).
# Point to your Philippines extract repo, e.g.:
# - Linux/macOS: FACILITIES_JSON_PATH=/absolute/path/to/davao-health-facilities.json
# - Windows:     FACILITIES_JSON_PATH=C:\path\to\davao-health-facilities.json
FACILITIES_JSON_PATH=

# Server port (default: 3000)
PORT=3000

# CORS: in production set to your frontend origin (e.g. https://your-app.vercel.app). Leave unset for * (dev).
# CORS_ORIGIN=

# WeatherAPI.com key for heat map (https://www.weatherapi.com/my/)
WEATHER_API_KEY=

# Meteosource.com key for per-point temperature by lat/lon (https://www.meteosource.com/client). If set, heat API uses Meteosource by default.
METEOSOURCE_API_KEY=
# Optional: limit parallel Meteosource requests to avoid rate limits (default 5). Lower to 2–3 if you get 429.
# METEOSOURCE_CONCURRENCY=5
# Optional: ms to wait before each uncached request (e.g. 200) to spread load.
# METEOSOURCE_DELAY_MS=0

# Supabase (Postgres) – optional. For heat_snapshots, CSV export, etc. Do not commit SUPABASE_SERVICE_ROLE_KEY.
# Get URL and service_role key from: Project Settings → API (https://supabase.com/dashboard/project/_/settings/api)
# SUPABASE_URL=https://<PROJECT_REF>.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=
# Optional fallback for Supabase URL (e.g. when same env is used by frontend): VITE_SUPABASE_URL=

# Pipeline report: upload/store latest heat-risk CSV for frontend download (no files in repo).
# Optional: require x-pipeline-report-key header on POST (set same value when running pipeline upload).
# PIPELINE_REPORT_WRITER_KEY=

# Supabase Edge Function: write heat snapshots (optional). Keep HEAT_WRITER_KEY server-side only; do not commit.
# HEAT_SNAPSHOT_WRITER_URL=https://<PROJECT_REF>.functions.supabase.co/heat-snapshot-writer
# Or set SUPABASE_PROJECT_REF=<your-project-ref> to build the URL.
# HEAT_WRITER_KEY=
